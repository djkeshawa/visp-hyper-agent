---
description: Drive Kit's preparation for the active feature until implementation is allowed.
---

Run the following via Bash:

```
visp plan
```

The composite executes exactly what Kit's `next` answer recommends and stops when a human is needed. If it stops, do what it names, then re-run.
