---
description: Check this machine and project: versions, pairing, generated assets, stale CI.
---

Run the following via Bash:

```
visp doctor
```

Fix what it names before continuing; a stale generated CI workflow breaks at the next pull request, not here.
