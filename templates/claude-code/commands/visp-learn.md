---
description: Propose a durable memory through the reviewed lifecycle (requires visp-memory).
---

Run the following via Bash:

```
visp learn "$ARGUMENTS"
```

This is a proposal, not a write: it becomes durable only after Memory's reviewed
lifecycle accepts it. A learn proposal records session learnings and does not complete a Kit task.
