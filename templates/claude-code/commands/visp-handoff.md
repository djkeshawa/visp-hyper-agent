---
description: Assemble the evidence for review: verify, review, assurance — as far as Kit allows.
---

Run the following via Bash:

```
visp handoff
```

The composite stops at the first step that needs a human decision (accepting a review is never automated). Do what it names.
