---
description: Start a piece of work: register the goal with Kit and prepare it as far as Kit allows.
---

Run the following via Bash:

```
visp new "$ARGUMENTS"
```

The composite executes exactly what Kit's `next` answer recommends — no stage
list lives on this side. When it stops ("waiting on you"), do what it names
(usually answering clarification questions), then run `/visp-plan` to continue.
